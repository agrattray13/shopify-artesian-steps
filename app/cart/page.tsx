import { CartPage } from "@/components/cart/cart-page";

export const metadata = {
  title: "Shopping Bag | Artesian Steps",
  description: "Review your selections and proceed to checkout.",
};

export default function Cart() {
  return <CartPage />;
}
